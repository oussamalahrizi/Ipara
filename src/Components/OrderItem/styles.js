import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    margin: 10,
    borderWidth: 1,
    borderRadius: 20,
    borderColor: "lightgray",
    marginHorizontal: 20,
  },
});
